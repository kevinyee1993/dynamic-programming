var reconstructQueue = function(people) {

  people.sort( (a,b) => {
    return a[0] < b[0];
  });



};

const input = [[7,0], [4,4], [7,1], [5,0], [6,1], [5,2]];

input.sort( (a,b) => {
  return a[0] < b[0];
});

console.log(input);
